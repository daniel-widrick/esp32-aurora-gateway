# Two OTA slots, because once this board is at the furnace there is no USB.
# Adding OTA later would require a partition-table change, which requires the
# USB cable we are about to lose - so it has to exist before the board leaves.
#
# otadata tracks the active slot and the rollback state. The app marks itself
# valid only after WiFi and the API come up (see main.c); if it can't, the
# bootloader falls back to the previous slot on the next reset.
#
# Name,     Type, SubType,  Offset,   Size
nvs,        data, nvs,      0x9000,   0x6000
otadata,    data, ota,      0xf000,   0x2000
phy_init,   data, phy,      0x11000,  0x1000
ota_0,      app,  ota_0,    0x20000,  0x300000
ota_1,      app,  ota_1,    0x320000, 0x300000
storage,    data, spiffs,   0x620000, 0x100000
